<!DOCTYPE html>

<title>UserRole</title>

{% block body %}
    <h1>UserRole</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_role.id }}</td>
            </tr>
            <tr>
                <th>Role_name</th>
                <td>{{ user_role.roleName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_role_index') }}">back to list</a>

    <a href="{{ path('app_user_role_edit', {'id': user_role.id}) }}">edit</a>

    {{ include('user_role/_delete_form.html.twig') }}
{% endblock %}
