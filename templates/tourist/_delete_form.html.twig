<form method="post" action="{{ path('app_tourist_delete', {'id': tourist.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tourist.id) }}">
    <button class="btn">Delete</button>
</form>
