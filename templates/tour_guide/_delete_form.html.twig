<form method="post" action="{{ path('app_tour_guide_delete', {'id': tour_guide.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tour_guide.id) }}">
    <button class="btn">Delete</button>
</form>
