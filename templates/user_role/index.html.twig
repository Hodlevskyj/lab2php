<!DOCTYPE html>

<title>UserRole index</title>

{% block body %}
    <h1>UserRole index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Role_name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user_role in user_roles %}
            <tr>
                <td>{{ user_role.id }}</td>
                <td>{{ user_role.roleName }}</td>
                <td>
                    <a href="{{ path('app_user_role_show', {'id': user_role.id}) }}">show</a>
                    <a href="{{ path('app_user_role_edit', {'id': user_role.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_user_role_new') }}">Create new</a>
{% endblock %}
