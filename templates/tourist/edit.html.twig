<!DOCTYPE html>

<title>Edit Tourist</title>

{% block body %}
    <h1>Edit Tourist</h1>

    {{ include('tourist/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_tourist_index') }}">back to list</a>

    {{ include('tourist/_delete_form.html.twig') }}
{% endblock %}
